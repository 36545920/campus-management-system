<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="top.campus.mapper.CourseMapper">
    <select id="selectCourseList" parameterType="top.campus.dto.CourseQueryDTO" resultType="top.campus.vo.CourseListVO">
        select id, course_name, course_code, remark, status, create_time
        from course
        <where>
            <if test="keyword != null and keyword != ''">
                and (course_name like concat('%', #{keyword}, '%') or course_code like concat('%', #{keyword}, '%'))
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
        order by create_time desc
    </select>

    <update id="updateCourse" parameterType="top.campus.dto.CourseUpdateDTO">
        update course
        <set>
            <if test="courseName != null and courseName != ''">course_name = #{courseName},</if>
            <if test="courseCode != null and courseCode != ''">course_code = #{courseCode},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = NOW()
        </set>
        where id = #{id}
    </update>
</mapper>
